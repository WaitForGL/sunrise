<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sunrise.mapper.LoginMapper">

    <resultMap type="com.sunrise.domain.user.User" id="UserResult">
        <result property="id"    column="id"    />
        <result property="username"    column="username"    />
        <result property="className"    column="class_name"    />
        <result property="majorName"    column="major_name"    />
        <result property="userType"    column="user_type"    />
        <result property="createTime"    column="create_time"    />
        <result property="status"    column="status"    />
    </resultMap>


    <sql id="selectUser">
        select id, username,class_name,major_name,user_type,create_time,status
        from user
    </sql>


    <select id="selectList" parameterType="com.sunrise.domain.user.User" resultMap="UserResult">
        <include refid="selectUser"/>
        <where>
            <if test="username != null "> and user_name = #{username}</if>
        </where>
    </select>


</mapper>